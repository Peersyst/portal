import { Button } from "@frontend/design-system-react/button";
import styled from "styled-components";

export const BridgeOriginTokenSelectorButton = styled(Button)(() => ({
    "&&": {
        padding: "0",
        minWidth: "auto",
    },
}));
