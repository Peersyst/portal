import { defaultDarkTheme } from "./default";
import { Themes } from "@frontend/design-system-core/themes";

export const themes: Themes = {
    default: {
        light: defaultDarkTheme,
        dark: defaultDarkTheme,
    },
};
