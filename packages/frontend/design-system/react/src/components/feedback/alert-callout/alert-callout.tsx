import { AlertCalloutRoot } from "./alert-callout.styles";
import { AlertCalloutProps } from "./alert-callout.types";

export function AlertCallout(props: AlertCalloutProps) {
    return <AlertCalloutRoot {...props} />;
}
