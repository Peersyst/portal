/**
 * Copied from node_modules/react-i18next/helpers.d.ts since the package doesn't have an export path.
 */
export type $Tuple<T> = readonly [T?, ...T[]];

/**
 * Copied from node_modules/i18next/helpers.d.ts since the package doesn't have an export path.
 */
export type $Dictionary<T = unknown> = { [key: string]: T };
