import common from "./common.json";
import languages from "./languages.json";
import errors from "./errors.json";

export const es = {
    translation: common,
    language: languages,
    error: errors,
};
